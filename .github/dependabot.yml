version: 2
updates:
  # marp-cli は mise (aqua backend) で管理しており Dependabot 非対応のため、
  # バージョン更新は mise.toml を手動で編集する。
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    cooldown:
      default-days: 7
