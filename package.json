{
  "name": "slides",
  "private": true,
  "engines": {
    "node": "24"
  },
  "scripts": {
    "dev": "marp --config-file .marprc.yml --server slides",
    "build": "marp --config-file .marprc.yml",
    "export": "marp --config-file .marprc.yml --pdf --allow-local-files"
  },
  "devDependencies": {
    "@marp-team/marp-cli": "^4.4.1"
  }
}
